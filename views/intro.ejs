<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Front Intro Page</title>
    <style>
        body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f5f5f5;
            font-family: Arial, sans-serif;
        }
        .intro-container {
            text-align: center;
            position: relative;
            z-index: 1;
            width: 100%;
            max-width: 450px;
        }
        .intro-heading {
            font-size: 3.5rem;
            margin-bottom: 5rem;
            opacity: 0;
            animation: fade-in 1s forwards 1s;
        }
        .buttons-container {
            opacity: 0;
            animation: fade-in 1s forwards 1.5s;
        }
        button {
            background-color: #4CAF50;
            color: white;
            padding: .3rem .7rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: fit-content;
            margin: 0 10px 5rem 5px;
            font-size: 2vw;
            opacity: 0.8;
            transition: opacity 0.3s ease-in-out;
        }
        button:hover {
            opacity: 1;
        }
        .intro-text {
            font-size: 1.5rem;
            margin-top: 1rem;
            opacity: 0;
            animation: fade-in 1s forwards 2s;
        }
        @keyframes fade-in {
            0% {
                opacity: 0;
                transform: translateY(100px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }
        video {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: 0;
            opacity: 0.6;
            animation: fade-in-background 20s linear infinite;
        }
       
        @media (max-width:1300px) {
            .intro-container {
                text-align: center;
                position: relative;
                z-index: 1;
                width: 100%;
                max-height: 75%;
            }
            .intro-heading {
                font-size: 3.5rem;
                margin-bottom: 6rem;
                opacity: 0;
                animation: fade-in 1s forwards 1s;
            }
            .buttons-container{
                margin-bottom: 4rem;
            }
            .intro-text{
                margin-bottom: 15rem;
            }
        }
        @media (max-width:650px) {
            .intro-container {
                width: 80%;
            } 
            .intro-heading {
                font-size:2.5rem;
        }
        .buttons-container{
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 1rem;
        }
        .intro-text{
            font-size: .8rem;
        }
    }
    </style>
</head>
<body>
    <video autoplay muted loop>
        <source src="/images/catering.mp4" type="video/mp4" >
    </video>
    <div class="intro-container">
        <h1 class="intro-heading">Welcome to JOBSCORNER</h1>
        <div class="buttons-container">
           <a href="/register"> <button id="register-btn">Register</button></a>
            <a href="/login"> <button id="login-btn">Login</button></a>
        </div>
        <div class="intro-text">
            We offer part-time job opportunities in various unskilled fields, such as catering, painting, and construction.
        </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const heading = document.querySelector('.intro-heading');
            const buttonsContainer = document.querySelector('.buttons-container');
            const introText = document.querySelector('.intro-text');
            const backVideo = document.querySelector("#video");

            backVideo.animate([
                { opacity:0 },
                { opacity:1 }
                ],{
                    duration: 6000,
                    easing:'ease-in-out',
                    fill: 'forwards'
                });

            heading.animate([
                { opacity: 0, transform: 'translateY(50px)' },
                { opacity: 1, transform: 'translateY(-10vh)' }
            ], {
                duration: 2500,
                easing: 'ease-in-out',
                fill: 'forwards'
            });

            buttonsContainer.animate([
                { opacity: 0, transform: 'translateY(50px)' },
                { opacity: 1, transform: 'translateY(-100px)' }
            ], {
                duration: 2500,
                easing: 'ease-in-out',
                delay: 500,
                fill: 'forwards'
            });

            introText.animate([
                { opacity: 0, transform: 'translateY(50px)' },
                { opacity: 1, transform: 'translateY(-100px)' }
            ], {
                duration: 2500,
                easing: 'ease-in-out',
                delay: 1000,
                fill: 'forwards'
            });
        });
    </script>
</body>
</html>